# res/

`openkrisp.png` がアイコンの元画像で、`openkrisp.ico` はそれを 16/20/24/32/40/48/64/128/256
の 9 サイズへ縮小して 1 つにまとめたものです。各サイズは PNG のまま格納しています
（Vista 以降が対応。256px を BMP で持つとファイルが無駄に大きくなるため）。

元画像を差し替えたら、次のコマンドで `.ico` を作り直してください。

```powershell
.\res\make-icon.ps1
```
